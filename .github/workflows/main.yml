name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install Backend dependencies
      working-directory: ./backend
      run: npm install

    - name: Run Backend Tests
      working-directory: ./backend
      run: NODE_ENV=test npm test

    - name: Build Docker Frontend
      run: docker build -t taskflow-frontend ./frontend

    - name: Build Docker Backend
      run: docker build -t taskflow-backend ./backend

    # Optional: Push to Docker Hub (Requires secrets)
    # - name: Log in to Docker Hub
    #   uses: docker/login-action@v3
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}
    # - name: Push Docker Images
    #   run: |
    #     docker tag taskflow-frontend ${{ secrets.DOCKER_USERNAME }}/taskflow-frontend:latest
    #     docker tag taskflow-backend ${{ secrets.DOCKER_USERNAME }}/taskflow-backend:latest
    #     docker push ${{ secrets.DOCKER_USERNAME }}/taskflow-frontend:latest
    #     docker push ${{ secrets.DOCKER_USERNAME }}/taskflow-backend:latest
